## What this changes

Briefly describe the change and the motivation.

## Checklist

- [ ] `cd backend && npm run typecheck && npm run build` passes
- [ ] `cd extension && npm run typecheck && npm run build` passes (if the extension changed)
- [ ] Updated `Docs/API.md` and `backend/openapi.yaml` together (if the API contract changed)
- [ ] Documented any new env var in `.env.example` and the README (if added)
- [ ] No secrets, real `.env`, build artifacts, or logs committed

## Notes for reviewers

Anything worth calling out — trade-offs, follow-ups, areas needing extra eyes.
